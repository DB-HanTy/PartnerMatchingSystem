<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hty.partnermatching.mapper.TeamMapper">

    <resultMap id="BaseResultMap" type="com.hty.partnermatching.model.domain.Team">
            <id property="id" column="id" />
            <result property="name" column="name" />
            <result property="description" column="description" />
            <result property="maxNum" column="maxNum" />
            <result property="expireTime" column="expireTime" />
            <result property="userId" column="userId" />
            <result property="status" column="status" />
            <result property="password" column="password" />
            <result property="createTime" column="createTime" />
            <result property="updateTime" column="updateTime" />
            <result property="isDelete" column="isDelete" />
    </resultMap>

    <sql id="Base_Column_List">
        id,name,description,maxNum,expireTime,userId,
        status,password,createTime,updateTime,isDelete
    </sql>
    <select id="selectTeamsByIds" parameterType="map" resultType="com.hty.partnermatching.model.domain.Team">
        SELECT id, name, description, maxNum, expireTime, userId, status, password, createTime, updateTime, isDelete
        FROM team
        WHERE isDelete = 0
        <if test="idList != null and !idList.isEmpty()">
            AND id IN
            <foreach collection="idList" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
    </select>

</mapper>
